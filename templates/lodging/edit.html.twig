{% extends 'base.html.twig' %}

{% block title %}Edit Lodging{% endblock %}

{% block body %}
    <h1>Edit Lodging</h1>
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }} 

        <div class="row">
        <div class="col">{{ form_row(form.number_rooms) }}</div>
        <div class="col">{{ form_row(form.max_people) }}</div>
        </div>
        <div class="row">
        <div class="col">{{ form_row(form.surface) }}</div>
        <div class="col">{{ form_row(form.weekly_base_price) }}</div>
        </div>

    {{ form_rest(form) }}
    <a class="btn btn-primary" href="{{ path('app_lodging_index') }}">back to list</a>

    {# {{ include('lodging/_delete_form.html.twig') }} #}


    
{% endblock %}

{% block javascripts %}
   
    
{% endblock %}
